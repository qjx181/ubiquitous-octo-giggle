# 项目二：招股说明书智能问答系统 — 面试问答全集

> 说明：本文档整合了项目二中涉及的高频面试问题与追问，按"架构→离线→在线→工程"的讲解顺序组织。每个回答采用面试者第一人称视角，结构上遵循"定义/背景 → 设计思路 → 实现细节 → 效果/收益 → 改进方向"的五段式框架。

---

## 第一部分：RAG 整体架构与概念

### Q1 请简要描述你项目的整体 RAG 流程

> 考察点：对 RAG 全链路的掌握程度

**回答：**

我的项目采用经典的两阶段 RAG 架构，分为离线管道和在线服务两部分。

**离线管道**负责知识库的构建，核心流程为：首先通过 MinerU 的 pipeline 后端对 PDF 文档进行解析，MinerU 自带版面分析模型，能自动识别正文/表格/图片/页眉页脚等元素，自动丢弃非正文内容，输出干净的结构化内容块；然后按章节对文本进行分块处理，每块约 800 字符，设置 150 字符的跨块重叠；接着使用 BGE-M3 模型将每个文本块编码为 1024 维的归一化稠密向量；最后将向量及其元数据导入 Milvus 向量数据库，采用 IVF_FLAT 索引结构。

**在线服务**负责用户查询的处理，核心流程为：首先对用户问题进行可选的 Query 理解（改写、消歧、子查询生成）；然后将问题编码为向量，在 Milvus 中执行相似度检索；可选地执行 BM25 全文检索并将两路结果通过 RRF 算法融合；可选地通过 BGE-Reranker 对结果进行重排序精排；最后将检索到的上下文与问题拼接成 Prompt，调用大语言模型生成答案。

**设计考量：** 两阶段的分离设计有两个核心收益。其一，离线管道可以批量处理、独立运行，构建一次知识库后在线服务即可重复使用；其二，在线服务中的每个环节都设计了智能降级机制，任何一个增强功能不可用都不会导致服务整体瘫痪。

---

### Q2 为什么选择 RAG 方案而不是 Fine-tuning？

> 考察点：对不同技术路线的理解深度

**回答：**

选择 RAG 而非 Fine-tuning 主要基于以下三方面考量。

**第一，知识热更新能力。** 我们的场景面向招股说明书，这类文档具有明确的时效性——季度财报发布后，旧数据即刻失效。RAG 方案只需替换知识库中的 PDF 文件即可完成知识更新，而 Fine-tuning 需要对模型进行重新训练，周期长、成本高。

**第二，可溯源和可信度。** 金融场景对信息的可验证性有严格要求。RAG 天然具备溯源能力——每个回答都可以追溯到具体的文档页码和章节，而 Fine-tuning 将知识压缩在模型参数中，无法向用户展示"答案来自哪里"。

**第三，成本与维护复杂度。** Fine-tuning 需要维护多个领域版本（招股书、财报、研报各有不同的知识边界），每次更新都可能引发灾难性遗忘。RAG 的知识与模型解耦，一套模型加多套知识库即可覆盖多个场景。

**需要说明的是：** RAG 并非完全没有短板。它的性能天花板受限于检索质量——如果检索不到相关信息，后续的生成环节必然受到影响。为了解决这一问题，我们在项目中引入了混合检索、Query 重写和多级降级策略来提升检索的鲁棒性。

---

### Q3 你认为 RAG 目前的主要瓶颈是什么？你的项目如何应对？

> 考察点：对 RAG 的深层次理解，而非停留在"会用"层面

**回答：**

我认为 RAG 当前主要有三个瓶颈，我的项目分别做了针对性设计。

**瓶颈一：检索质量。** 这是 RAG 最核心的挑战——如果检索阶段未能召回相关信息，LLM 将面临"垃圾进垃圾出"（Garbage In, Garbage Out）的局面，最终生成不可靠的答案。

- 我的应对方案是多路召回加精排：向量检索（语义匹配）+ BM25 全文检索（关键词匹配），通过 RRF 融合后再由 Cross-Encoder 精排，从多角度提升召回率和排序质量。
- 同时引入了 Query 理解模块，对模糊问题进行改写和消歧，提升检索的命中率。

**瓶颈二：上下文窗口限制。** LLM 的上下文长度有限，检索到的文档片段数量不能无限制地喂给模型。

- 我的策略是分阶段压缩：Milvus 先召回 top_k=10 的候选集，RRF 融合后降至 top_k=6，Reranker 精排后只保留 top_k=3 给 LLM。通过层层筛选，确保进入 LLM 的是质量最高的上下文。

**瓶颈三：多跳推理能力不足。** 当用户的问题需要融合多个文档片段的信息时，单次检索往往难以覆盖全部需要的信息。

- 这是一个待改进的方向。目前我们通过 Query 理解模块生成子查询来部分解决，但要真正解决多跳推理问题，需要引入更复杂的 Agent 路由机制，这也是后续迭代的重点方向。

---

### Q4 PDF 解析过程中遇到过哪些难点？如何解决？

> 考察点：实际工程经验，是否踩过坑

**回答：**

PDF 解析过程中主要遇到四个典型难点，每个都对应了具体的技术方案。

**难点一：多栏排版导致文本顺序错乱。** 招股说明书普遍采用双栏排版，早期用 PyMuPDF 按坐标位置提取文本，左栏和右栏的文本会交错排列，导致分块后语义不连贯。

- 解决方案：改用 MinerU 的 pipeline 后端，它自带版面分析模型，能自动识别多栏布局并按阅读顺序输出文本。

**难点二：表格与文本混合。** 表格数据是结构化信息（行 × 列），如果直接当作纯文本提取，行列对应关系会丢失。

- 解决方案：MinerU 的版面分析模型自动识别表格区域，以独立内容块输出，保留完整的行列结构，表格和文本自然分离。

**难点三：页眉页脚干扰章节识别。** 页码、页眉（如"第三章"）会出现在每一页的固定位置，干扰章节检测算法。

- 解决方案：MinerU 自动识别并丢弃 page_header、page_footer、page_number 等非正文元素，输出的 344 个内容块全部是干净的正文。

**难点四：扫描件不可解析。** 早期用 PyMuPDF 只能解析文本型 PDF，对扫描图片型 PDF 无能为力。

- 解决方案：改用 MinerU，它内置 OCR 能力，能处理扫描件 PDF，自动识别图片中的文字内容。

---

## 第二部分：离线管道

### Q5 分块大小为什么选择 800 字符？这个数值是怎么确定的？

> 考察点：参数选型的工程依据

**回答：**

800 字符这个数值是由两方面的约束共同决定的。

**第一，编码器的 token 限制。** 我们使用的 BGE-M3 模型的最大输入长度为 512 个 token。以中文场景计算，一个 token 大约对应 1.5-2 个字符，512 token 约等于 800-1000 字符。为了确保不会触发模型截断（截断会丢失句末信息），我们取了下限约 800 字符。

**第二，语义完整性的考量。** 如果分块太小（如 256 字符），一个完整的段落被切成若干碎片，单个分块的语义不足以支持精确的语义检索。如果分块太大（如 1024 字符），超出模型输入上限后尾部信息丢失。800 是平衡这两个因素后的经验值。

**需要补充的是：** 这个数值不是一成不变的。如果后续换用支持更长上下文的编码模型（如 BGE-M3 支持 8192 token 的版本），我们会相应地调大分块大小以保留更完整的段落语义。

---

### Q6 Overlap 为什么设置为 150 字符？为什么不是简单地按字符数硬切？

> 考察点：对边界处理的理解

**回答：**

150 字符的 overlap 设计有明确的工程依据。

**数值依据：** 800 字符 × 20% = 160 字符，约等于 1-2 个中文句子的长度。选择 150 字符是一个取整后的近似值，核心考量是确保一个完整句子的信息不会因为切分而丢失在分块边界。

**切割策略：** 我们没有采用简单的前 800 字符一刀切的策略，而是使用 `rfind("。")` 从句号位置进行软切割。这样可以避免从句子中间腰斩——如果一个句子在第 765 字符处开始、在第 815 字符处结束，硬切会在 800 处把句子打断，而软切会找到第 800 字符之前的最后一个句号，从那里作为切分点。overlap 确保了这个被切到后半段的句子至少完整地出现在下一个分块的开头。

**特殊处理：** 表格数据不走 overlap 逻辑。因为表格的每一行本身就是一个独立的数据点，添加 overlap 只会重复表头信息，不会带来额外的语义收益。

---

### Q7 如果没有 overlap，会出什么问题？

> 考察点：能否从反面角度理解设计的原因

**回答：**

没有 overlap 会导致分块边界处的信息丢失，这是文本分片中最常见但也最隐蔽的问题。

举例来说，假设原文中有这样一段连续的文本：一个句子的前半部分落在分块 A 的末尾（第 780-810 字符），后半部分落在分块 B 的开头（第 810-840 字符）。由于分块 A 在 800 字符处被截断，它包含的是"该公司的营业收入为"——这是一个不完整的陈述。分块 B 从第 801 字符开始，包含"1.2亿元，同比增长15%"——同样是缺乏主语的碎片。

当检索时，无论用户查询的是"营业收入是多少"还是"增长率是多少"，这两个分块各自都无法提供完整的上下文。Overlap 确保了这类跨越分块边界的信息至少在一个分块中是完整的。

---

### Q8 表格为什么单独处理？不处理会怎样？

> 考察点：对不同数据类型处理的区分意识

**回答：**

表格是结构化数据，与连续文本有本质区别。当作文本直接提取时，表格的行列对应关系会丢失。例如一个三列五行的财务数据表，提取后变成一长串平铺的文本："营收100亿成本60亿利润40亿营收120亿成本70亿利润50亿……"，读者无法分辨"100亿"对应的是营收还是成本。

在我们的方案中，MinerU的版面分析模型能自动识别表格区域，将表格与普通文本分离处理。表格的每一行作为一个独立的数据点，保留其"列名:值"的结构化格式存入知识库。这样在检索时，针对"2024年营业收入"的查询可以直接匹配到对应的表格行。

---

## 第三部分：在线服务

### Q9 请描述 RetrievalService 中自动探测机制的设计思路

> 考察点：容错设计意识

**回答：**

自动探测机制的核心思路是"宁可少一个功能，也不让服务崩溃"。具体来说，在 RetrievalService 初始化时，依次调用四个 `_probe_xxx` 方法分别探测 Query 理解、Redis 缓存、BM25 混合检索和 BGE-Reranker 这四个增强功能的可用性。每个探测方法都独立包裹在 try-except 块中，无论探测过程中出现什么异常——模块不存在、网络不通、配置文件缺失——都不会影响服务的启动和正常运行。

探测结果存储在 `self.features` 字典中，后续运行时通过检查这个字典来决定是否调用该功能。举例来说，Redis 缓存功能探测到连接失败，`features["cache"]` 保持为 False，后续的 query 请求会跳过缓存检查/写入逻辑，不影响检索和生成的主流程。

**选择自动探测而非手动配置的原因：** 手动配置的开关需要运维人员了解每个功能的依赖条件，而且配置修改后需要重启服务才能生效。自动探测让系统自适应运行环境——今天 Redis 服务没启动，缓存功能自动跳过；明天 Redis 恢复了，下次请求时缓存功能自动启用——真正做到了零配置运维。

---

### Q10 Query 理解模块失败时为什么不阻断后续检索？

> 考察点：降级策略的设计哲学

**回答：**

这是一个经过权衡的设计决策。Query 理解在我们的架构中定位为"锦上添花"的功能，而非"守门员"。它负责改写表述模糊的查询、补充缩写、生成子查询——这些操作如果成功，能提升检索精度；但如果失败了，使用原始查询继续检索仍然能产生可用（尽管可能稍差）的结果。

我们的设计原则是：**任何单步功能的失败都不应该导致整个请求处理失败。** 如果 Query 理解被设计成"守门员"——它失败了业务就拒绝处理——那么当用户的一个完全合理的问题因为 Query 理解模块的临时异常而被拒之门外时，用户体验会很差。

因此我们采用宽松的失败策略：Query 理解报错时静默记录日志，使用原始查询继续执行后续的编码、检索和生成流程。这种设计认为"一个不够精准的答案好于没有答案"。

---

### Q11 混合检索中为什么用 RRF 而不是加权平均？

> 考察点：对融合算法的理解深度

**回答：**

这个问题的关键在于**分数可比性**。加权平均要求两路检索输出的分数处于同一量级和分布，但在实际中，这几乎不可能满足。

具体来说，向量检索输出的余弦相似度范围在 0 到 1 之间，是一个有界的概率度量。而 BM25 的原始分数取决于词频、文档长度和语料库的统计特征，理论上可以输出从 0 到几十甚至上百的值。当分数量级不一致时，加权平均的融合结果实际上等价于权重更大的那一路——因为 BM25 的几十分会完全压制向量检索的 0.8 分。

RRF（Reciprocal Rank Fusion）巧妙地绕过了这个问题：它不直接对分数做加权，而是对**排名**做加权。公式为 `score = weight / (k + rank)`，无论两路检索的原始分数数值怎么变化，排名始终是 1、2、3……这样的有序整数。这样两路检索的贡献就在同一个框架下进行比较了。

在实现中，k 值设为 60，这是一个控制"排名尾部影响力"的平滑参数。较小的 k 值会放大高排名结果的权重差异，较大的 k 值则会让排名靠后的结果也有一定的竞争力。

---

### Q12 Reranker 为什么采用异步预加载？为什么 probe 阶段不直接加载模型？

> 考察点：对启动流程优化的理解

**回答：**

这个设计源于一个实际问题：BGE-Reranker 模型加载到 GPU 需要数秒时间，如果放在 __init__ 中同步加载，服务的启动时间将从秒级延长到 5-10 秒，而且如果模型文件损坏或兼容性出问题，整个服务会启动失败。

我们的方案分为三个阶段：

**第一阶段：Probe 探测（毫秒级，__init__ 中执行）。** 只检查模型缓存文件在磁盘上是否存在，不真正加载到内存或 GPU。探测本身是轻量级的文件路径检查，即使模型文件有问题也不会影响服务启动。

**第二阶段：异步预加载（后台线程，不阻塞 __init__）。** probe 通过后在后台线程中调用 `_async_preload_reranker()` 真正加载模型到 GPU。加载成功则更新 `self.reranker` 实例，加载失败则只记录日志、不阻塞主线程。

**第三阶段：懒加载兜底（首请求时执行）。** 如果第一个用户请求到达时后台加载尚未完成，`_lazy_load_reranker()` 会等待最多 60 秒。如果超时仍未完成，则尝试同步加载一次。同步加载也失败时，将该功能标记为不可用，跳过重排序步骤。

这种设计确保了：无论模型加载是否成功，服务都能在秒级完成启动并对外提供服务。这是一个典型的"优化延迟的启动体验，容忍功能的逐步就绪"的工程实践。

---

### Q13 缓存策略是如何设计的？为什么不缓存需要澄清的 Query？

> 考察点：缓存策略的工程考量

**回答：**

缓存策略的核心设计是：**只缓存有检索结果的查询，不缓存中间状态。**

缓存键的生成方式为 `MD5(question | content_type | top_k)`，将问题文本、类型过滤条件和检索参数拼接后取哈希。当缓存命中时，直接返回历史结果，跳过完整的检索+生成管道，响应时间从数秒降至毫秒级。缓存项的生存时间由配置文件中的 `CACHE_TTL_SECONDS` 控制。

**关于"不缓存需要澄清的 Query"的设计原因：** 当 Query 理解模块判断用户问题过于模糊时，返回的是一段模板化的澄清文本（如"您想了解哪个时间段的财务数据？"），这不是计算结果，而是引导用户补充信息的交互提示。这类提示文本没有计算成本，缓存它没有意义。更重要的是，用户补充信息后会提交一个新的、更具体的问题，这个新问题的缓存键与之前的模糊问题的缓存键不同，并不会命中缓存。

---

### Q14 同步接口和流式接口在设计上的核心区别是什么？

> 考察点：对不同交互模式的理解

**回答：**

两者的核心区别在于**交互模型的时序**——同步接口是一次性交付完整结果，流式接口是分阶段逐步交付。

**同步接口（POST /chat/）** 在内部等待所有步骤完成后一次性返回 JSON 响应，包含 answer 和 sources 两个核心字段。它的优点是调用方处理逻辑简单，适合后端系统之间的集成、非交互式的 API 调用等场景。

**流式接口（POST /chat/stream）** 采用 SSE（Server-Sent Events）协议，分两个阶段推送数据：第一阶段推送引用来源（sources，以 JSON 格式嵌入 SSE 事件中），第二阶段逐 token 推送大模型生成的答案文本。这种模式的核心价值在于提升用户的感知响应速度——用户可以看到来源先于答案展示出来，在心理上建立了对回答的信任，然后逐字看到答案的"生成过程"而非"等待结果"。

**关于数据流的差异：** 两个接口的检索管道是完全相同的（Query 理解 → 编码 → 检索 → 融合 → 排序），区别只在于 LLM 生成阶段——同步接口调用 `generate()`（完整等待返回），流式接口调用 `generate_stream()`（逐 token yield）。

---

### Q15 检索结果为空时，为什么选择抛出异常而不是返回空答案？

> 考察点：异常处理的设计哲学

**回答：**

这是一个关于"系统如何应对无结果场景"的决策，我们的设计原则是：**宁可告诉用户我们不知道，也不让模型在缺乏依据的情况下编造答案。**

当检索结果为空时，如果继续将空的上下文喂给 LLM，模型在缺乏参考信息的情况下有两种可能的反应：一是诚实地回答"我不知道"，二是基于自身的预训练知识编造一个看似合理的答案——后者是 RAG 系统中需要极力避免的幻觉（Hallucination）问题。

因此我们选择在上述情况下抛出 `BusinessException(ErrorCode.RETRIEVAL_EMPTY)`，前端捕获这个异常后展示"未找到与问题相关的信息"的统一提示。这个流程让前端能够以统一的方式处理"无结果"场景，同时也保证了答案的可信度——如果 LLM 回答了，那一定有引用依据。

值得注意的是，这种设计在流式接口中有不同的表现——流式接口不抛异常，而是直接 yield "未找到与问题相关的信息"文本。原因在于 Generator 异常在 Python 的异步生成器中的传播路径复杂，不如直接 yield 提示信息让前端展示来得直接可靠。

---

## 第四部分：LLM 路由与调用

### Q16 为什么要做智能分流？不分区行不行？

> 考察点：资源调度的工程思维

**回答：**

智能分流的核心驱动力是**资源约束驱动的差异化服务**。我们的硬件环境是一张 RTX 4060（8GB 显存），无法同时高效运行多个大模型。同时，并非所有用户的查询都需要最强的模型来处理——有些问题（如"第15页是什么内容"）用轻量模型就能很好回答，而有些问题（如"分析公司近三年的营收趋势"）需要更强的推理能力。

如果我们不分区，只有两个选择：
- 全部走大模型：推理速度慢、显存压力大、并发能力低。
- 全部走轻量模型：简单问题响应快，但复杂问题的回答质量不能满足需求。

智能分流解决了这个两难问题：_analyze_complexity 方法通过关键词匹配、问题长度、对话历史等特征将用户查询分类为"简单"或"复杂"，简单问题分配给 Ollama（本地轻量模型，秒级响应），复杂问题分配给 SGLang（本地高性能模型，深度推理）。

**目前的局限性：** 当前的分流策略是基于规则（关键词+正则）的启发式方法，准确率大约在 80-90%。后续的改进方向是训练一个小型分类模型来替代规则，或者让用户手动选择"快速回答"和"深度分析"两种模式。

---

### Q17 create_client 中为什么用 __new__ 而不是直接调用构造函数？

> 考察点：对 Python 对象创建机制的理解

**回答：**

这是一个由依赖注入方式决定的技术方案。LLMClient 的 `__init__` 构造函数从全局配置 `settings.LLM_PROVIDER` 中读取 provider 并初始化对应的 API 地址、模型名和鉴权信息——这意味着一次构造只能创建一个特定配置的实例。

但 LLMRouter 需要同时持有两个不同配置的客户端实例（分别对应 SGLang 和 Ollama），以便在运行时根据查询复杂度动态选择和切换。如果使用 `LLMClient()` 构造函数，创建第二个实例时会覆盖第一个实例的 provider 配置。

因此我们采用 `LLMClient.__new__(LLMClient)` 的方式绕过 `__init__`，创建一个"空白"的对象实例，然后手动为每个属性赋值。这样我们可以在不完全依赖构造函数的前提下，灵活地构造具有不同配置的 LLMClient 实例。

**技术评价：** 这本质上是"先让它跑起来"的技术方案。从代码质量的角度看，更合理的做法是让 LLMClient 的 `__init__` 接受 provider 作为参数，而不是依赖全局配置。这也是我们在代码待修复清单中记录的问题之一。

---

### Q18 LLMClient 中为什么对 4xx 错误不进行重试？

> 考察点：重试策略的设计边界

**回答：**

重试策略的核心原则是：**只重试瞬态故障，不重试永久故障。**

HTTP 错误可以分为两类：4xx 状态码表示客户端请求本身有问题（如 401 未授权、404 请求的资源不存在、429 请求频率超限），这类错误意味着请求需要修改后才能成功，重试无论多少次都会得到相同的错误结果。5xx 状态码或网络层面的错误（连接超时、连接被拒绝）表示服务端临时不可用，这类错误很可能在数秒内自行恢复，重试是有意义的。

因此我们的实现中，`except httpx.TimeoutException` 和 `except httpx.ConnectError` 会触发指数退避重试（2^attempt 秒等待），而其他类型的异常——包括 HTTP 状态码错误和响应体解析错误——直接向上抛出，不做重试尝试。

**指数退避选择 2^attempt 的原因：** 这是业界广泛采用的标准策略。第一次重试等 1 秒、第二次等 2 秒、第三次等 4 秒——快速拉开间隔避免在服务恢复初期造成"惊群效应"。如果采用线性退避（1 秒、2 秒、3 秒），第三次尝试时服务可能仍处于短暂不可用状态，导致无效重试。

---

### Q19 为什么将检索到的上下文拼入 System Prompt 而非 User Message？

> 考察点：对 LLM 行为模式的理解

**回答：**

这个设计决策基于 LLM 对不同角色消息的权重差异。在大多数商用和开源 LLM 中，system prompt 通常具有比 user message 更高的语义权重——模型在回答时会更倾向于遵循 system prompt 中的指令和约束。

将上下文拼入 system prompt 的尾部，相当于告诉模型："以下是你必须参考的信息，请基于它来回答用户的问题。"这样模型在生成回答时会自然地围绕检索到的文档片段展开。

如果将上下文放在 user message 中，和用户的原始问题混在一起，模型可能会将上下文视为对话历史的一部分——它可能会"回答"上下文中的内容，而不是基于上下文来回答用户的问题。

**需要说明的是：** 这个结论并非通用的经验法则。部分模型对 system 和 user 的角色区分不够明确，部分模型（如 Llama 3）的 system prompt 支持度有限。我们当前的做法是一个经过场景验证的经验设计，未来如果更换底层模型，可能需要重新评估这个策略。

---

### Q20 流式接口为什么没有设计重试机制？

> 考察点：流式场景与传统请求-响应模式的差异认知

**回答：**

流式场景和传统的请求-响应模式存在一个本质差异：**数据一旦发送就无法撤回。**

在非流式场景中，如果请求超时或连接失败，可以重新发起一次完整请求，因为没有任何部分响应被发送给客户端。但在流式场景中，大模型可能在生成了 80% 的答案后连接中断——前 80% 的 token 已经被逐段传递给前端并展示给用户。如果此时执行重试，新的流式请求会从第一个 token 开始重新生成，前端将收到"营业收入为100亿元"和"营业收入为120亿元"两段内容，用户看到的是混乱的、重复的回答。

因此我们在流式接口中放弃了重试机制，而是依赖上层的 LLMRouter 进行引擎级故障转移——当主引擎（如 SGLang）的流式生成失败时，切换到备用引擎（Ollama）重新开始完整的生成流程。这种设计避免了 token 级别的重复，但代价是用户需要重新等待一次完整的生成过程。

---

## 第五部分：工程实践与设计决策

### Q21 为什么自行封装 LLMClient 而不使用 LangChain 等框架？

> 考察点：对框架取舍的独立思考能力

**回答：**

两个层面的考量：业务复杂度和技术可控性。

**业务复杂度层面：** 我们需要 LLMClient 做的事情非常明确——接收问题和上下文，拼接成对话消息，通过 HTTP 调用 LLM API，解析返回结果。这个流程的核心逻辑不到 100 行代码就能实现。LangChain 虽然提供了丰富的链式抽象和模型集成，但对于我们这个规模的业务需求，引入整套框架会带来不必要的依赖体积、学习成本和调试复杂度。

**技术可控性层面：** 使用 LangChain 等封装框架，调用链中的每个环节（请求构建、重试策略、错误处理、响应解析）都隐藏在了框架的黑盒中。当生产环境中出现异常时——例如某个 LLM 提供商返回了非标准的错误响应格式——我们需要深入理解框架的内部实现才能定位问题。自己封装意味着每条代码路径都在掌控之中，出现问题可以快速定位和修复。

**对不同场景的看法：** 如果项目需要快速对接多个 LLM 提供商、需要复杂的 Agent 工作流编排、或者团队对 LLM 调用层没有特殊的定制需求，使用 LangChain 是合理的选择。我们选择自己封装是因为我们的需求范围有限，且对可控性的要求较高。

---

### Q22 单例模式下的竞态条件问题如何处理？

> 考察点：对并发场景下代码安全性的意识

**回答：**

当前 `get_retrieval_service()` 的实现是一个简单的"if 单例是否存在，不存在则创建"的模式。在 FastAPI 的异步处理模型下，如果多个请求在服务启动的瞬间几乎同时到达，理论上存在多个协程同时判断 `_retrieval_service is None` 为 True、然后各自创建一个 RetrievalService 实例的竞态条件。

虽然 Python 的 GIL（全局解释器锁）保证同一时刻只有一个线程在执行 Python 字节码，但在异步 I/O 场景中，协程的切换不是由 GIL 控制的——一个协程在 `__init__` 的 probe 阶段执行到 await/I/O 操作时，控制权可能切换给另一个协程，此时第二个协程也可能看到 `_retrieval_service` 仍为 None。

**改进方案：** 理想的方案是在 FastAPI 的 `lifespan` 事件（启动时）中显式创建 RetrievalService 实例，这样在用户请求到达之前，服务实例已经被创建好并赋值给全局变量，不存在竞态条件。或者使用 `asyncio.Lock()` 保护创建逻辑，确保同一时间只有一个协程执行实例化过程。

---

### Q23 如果 Milvus 或 LLM 服务在运行时重启，系统能自动恢复吗？

> 考察点：对系统稳定性的全面思考

**回答：**

当前版本不支持运行时的自动重连，这是单例模式的一个已知局限性。RetrievalService 在初始化时获取 Milvus 客户端和 LLM 客户端的实例，如果后续这些后端服务发生重启，内部持有的客户端连接会变为失效状态。

**当前的响应对策：** 外层调用（chat.py 中的异常处理）在捕获到数据库连接错误或 LLM 调用失败时，会向客户端返回 500 状态码和具体的错误信息。系统不会崩溃，但后续所有请求都会持续失败，直到服务端重新部署。

**改进方向有两个：**
1. **连接池+心跳检测模式**：让 Milvus 客户端和 HTTP 客户端支持自动重连，在每次调用前检查连接状态，发现断连时自动重建连接。
2. **服务注册与发现**：将后端服务的可用性检测从初始化时的一次性操作改为周期性的健康检查，并在检测到服务恢复时重建客户端实例。

第一种方案实现成本较低，适合当前的项目规模；第二种方案更适合分布式微服务架构。我们计划在后续迭代中实现第一种方案。

---

## 第六部分：检索管道辅助模块

### Q24 BM25 和 TF-IDF 有什么区别？你的项目中 k1、b 参数如何确定？

> 考察点：对经典检索算法的理解深度

**回答：**

BM25 是 TF-IDF 的改进版本，主要解决了两个问题。

**第一，词频饱和问题。** TF-IDF 的词频（TF）是线性的——"营收"在文档中出现 50 次的得分是 10 次的 5 倍。但实际上词频增加到一定程度后，信息增益是递减的。BM25 通过参数 k1（默认 1.5）实现了词频的饱和函数——高频词带来的加分被压低，避免了高频词过度主导排序结果。

**第二，文档长度偏差。** 长文档天然包含更多词汇，TF-IDF 下长文档得分偏高。BM25 通过参数 b（默认 0.75）对文档长度进行归一化——文档越长，惩罚越重。

**参数确定方法：** k1=1.5 和 b=0.75 是 BM25 算法在长文档场景（如新闻、学术论文）中被广泛验证的标准推荐值。k1 的推荐范围是 1.2-2.0，b 是 0.5-0.75。这两个值可以通过网格搜索在验证集上调优，但我当前项目中采用了业界标准值，效果已经满足需求。

**面试话术：** "BM25 比 TF-IDF 好的核心原因是它解决了两个偏差——词频偏差和长度偏差。k1 让高频词得分饱和，b 让长文档受到合理惩罚。"

---

### Q25 BM25 在什么场景下比向量检索效果更好？

> 考察点：对两种检索方式优劣的对比理解

**回答：**

BM25（关键词检索）和向量检索（语义检索）各有优势，BM25 在以下四个场景中表现明显优于向量检索：

**精确编号匹配：** 如 "1-1-1"、"第 III-A-2 条" 这类格式固定的编号。向量检索基于语义理解，会将 "1-1-1" 编码为语义向量，很难与 "第 III-A-2 条" 区分。BM25 直接做精确的关键词匹配，对这些编号的匹配准确率接近 100%。

**罕见专业术语：** 招股说明书中特有的术语（如"加权平均净资产收益率"），在训练数据中出现频率低，向量编码时容易被稀释。BM25 直接按词匹配，不受术语频率影响。

**否定表达式：** "不包含在合并报表范围内"——向量检索可能理解"范围内"的语义而忽略"不"的否定含义，BM25 能精确匹配到"不"和"包含"这两个关键词。

**短查询精确匹配：** 如 "第 15 页"、"营业收入" 这类极短的精确查询。向量检索对短文本的语义理解能力有限，BM25 的精确匹配反而更可靠。

**面试话术：** "BM25 和向量检索不是替代关系，是互补关系。所以我的项目做了混合检索，用 RRF 融合两路结果，兼顾语义理解和精确匹配。"

---

### Q26 Bi-Encoder 和 Cross-Encoder 的具体区别是什么？为什么选 bge-reranker-base？

> 考察点：对重排序模型的选型和原理理解

**回答：**

**两者的本质区别：**能否捕捉 Query 和 Doc 之间的交互信息。

**Bi-Encoder（双塔模型）** 将 Query 和 Doc 分别编码为独立的向量，然后计算向量相似度（如余弦相似度）。优点是 Doc 向量可以预计算和缓存，检索时只需编码 Query，速度快。缺点是 Query 和 Doc 之间没有交互——"苹果好吃"和"苹果手机"中的"苹果"在向量空间中是同一个点，Bi-Encoder 无法区分。

**Cross-Encoder（交叉编码器）** 将 Query 和 Doc 拼接后一起编码，模型能同时看到两者的全部信息，捕捉细粒度的语义交互。精度更高，但无法预计算——每个 Query-Doc 对都要独立编码，速度慢。

**两阶段使用策略：** 先用 Bi-Encoder（向量检索）从全库中粗筛出 top_k=10 的候选集，再用 Cross-Encoder 精排选出 top_k=3 给 LLM。兼顾速度和精度。

**为什么选 bge-reranker-base：** 选型基于三个约束——(1) 中文场景效果好；(2) 模型大小约 1GB，在 8GB 显存的 RTX 4060 上能流畅运行；(3) BGE 系列从 embedding 到 reranker 模型族内兼容性好，有配套的 bge-m3 embedding 模型。如果显存更大可以用 large 版（精度更高但更慢），如果更追求速度可以用 small 版。

---

### Q27 MVCC -- MILVUS 为什么用 HTTP 连接而不用 gRPC？为什么选 IVF_FLAT 不选 HNSW？

> 考察点：向量数据库选型的工程考量

**回答：**

**关于连接协议（HTTP vs gRPC）：**

我的开发环境是 WSL（Windows Subsystem for Linux），网桥模式下 gRPC 有时会出现网络桥接问题。Milvus 2.3+ 版本提供了 HTTP 协议支持，比 gRPC 更容易调试——可以用 curl 直接测试连接状态，出现问题更容易定位。

生产环境中，gRPC 的性能更好（基于 Protobuf 的二进制传输、支持流式调用），建议切换到 gRPC 协议。当前用 HTTP 是开发环境的折衷选择。

**关于索引选型（IVF_FLAT vs HNSW）：**

关键因素是数据量级。我的项目当前知识库约 500-800 条文档，在这个量级下：

- IVF_FLAT（nlist=128）：精度接近暴力搜索（FLAT），速度快，内存开销小。
- HNSW：内存是原始向量的 5 倍以上，对几百条数据是"杀鸡用牛刀"。

如果数据量增长到十万级、百万级，HNSW 的高内存开销换来的检索速度优势才值得。索引选型取决于数据量级，不是越高级越好——这是面试中值得主动强调的工程思维。

---

### Q28 缓存架构中 L1 和 L2 分别指什么？为什么 L1 没有实现？

> 考察点：对缓存分层的理解

**回答：**

文档中提到了一二级缓存的设计：L1 是本地内存缓存（应用进程内），L2 是 Redis 分布式缓存。

当前**只实现了 L2（Redis 缓存）**，L1 尚未实现。原因是 L2 缓存已经能满足当前场景的需求——项目是单机部署，没有多实例共享缓存的问题。L1 内存缓存的实现价值有限：

- 如果先查 L1 再查 L2，会增加代码复杂度和延迟
- 在单实例部署下，L2 Redis 的延迟已经很低（毫秒级）
- 生产环境多实例部署时，L1+L2 两级缓存的优势才体现出来——L1 本地缓存可以避免每个实例都去查 Redis

这是一个"知道更好的方案但当前没必要做"的设计决策。

**可主动指出的代码瑕疵：** 当前 `redis_client.py` 中多处使用了 `print()` 代替 `logger`（共 5 处）。在线服务代码中不应使用 print，因为它无法按日志级别过滤、无法轮转、无法集中收集。这是答辩后计划修复的事项之一。

---

### Q29 Query 理解模块为什么采用纯规则方案而不是模型方案？

> 考察点：技术选型的权衡能力

**回答：**

选择纯规则而非模型方案，是基于三个考量。

**第一，场景覆盖度。** 招股说明书的问题是领域特定的——用户主要问财务数据、风险因素、业务介绍等。通过关键词和正则规则已经可以覆盖绝大部分场景。规则方案在这个受限领域内够用了。

**第二，可解释性和可维护性。** 规则方案的每个判断都是显式的——"为什么分成财务类？因为问题里有'营收'这个词"。面试官问起来可以清楚地解释每一条判断逻辑。模型方案则是一个黑盒，说不清楚分类依据。而且添加新意图只需要加几条正则，不需要重新训练模型。

**第三，延迟和成本。** 规则匹配是毫秒级、零额外成本。模型方案需要调用一个分类模型或 LLM，增加延迟和 API 调用成本。

**局限与改进方向：** 规则方案的泛化能力差，换到法律、医疗领域这套规则就废了。后续改进方向是在规则基础上加一个小型分类模型做意图识别，规则兜底、模型提升，两者互补。

---

### Q30 Embedding 服务中为什么要对向量做归一化？模型加载失败时和 Reranker 的处理方式有何不同？

> 考察点：对 Embedding 细节和容错策略的理解

**回答：**

**关于向量归一化：**

`normalize_embeddings=True` 的作用是对向量进行 L2 归一化，使其模长变为 1。归一化后，两个向量的内积就等于余弦相似度。

不归一化的问题：不同长度的文档编码后，向量的模长不同。长文本的向量模长更大，计算内积时天然大于短文本向量，检索结果会偏向长文档——"不是因为更相关，只是因为更长"。归一化消除了这种不公平性。

**关于模型加载失败的容错策略——和 Reranker 的本质区别：**

Embedding 和 Reranker 模型加载失败时处理方式完全不同，原因在于它们在 RAG 流程中的定位：

- **Reranker 是锦上添花：** 没有它，系统只是精度降低，直接用原始检索结果排序即可。所以可以采用三级降级策略（probe→异步预加载→懒加载兜底），全部失败则优雅降级跳过重排序步骤。
- **Embedding 是雪中送炭：** 没有向量根本无法进行检索——整个 RAG 流程的基石。所以必须抛异常让上层知道，不能静默降级。这是一个设计原则：核心组件出错了不能偷偷摸摸跳过，必须让调用方意识到系统不可用。

---

## 面试应答节奏参考

| 问题类型 | 回答结构 | 建议时长 | 示例 |
|---------|---------|---------|------|
| 概念题（问"是什么"） | 一句话定义 → 核心原理 → 项目中的具体应用 → 效果评估 | 1-2 分钟 | "RAG 是……，核心原理是……，我在项目中通过……实现，效果是……" |
| 设计题（问"为什么"） | 需求背景 → 可选方案对比 → 选型理由 → 具体实现参数 | 1-2 分钟 | "当时面临……的约束，比较了 A 和 B 方案，选择 A 的原因有三……" |
| 参数题（问"为什么是这个值"） | 硬性约束 → 经验推导 → 计算公式 → 敏感性分析 | 30 秒-1 分钟 | "编码器上限 512 token，按中文字符约 1.6 倍换算得到 800，再配合 20% overlap……" |
| 对比题（问"A 与 B 的区别"） | 核心差异 → 各自优缺点 → 适用场景对比 → 项目中的选择 | 1 分钟 | "双塔独立编码快但损失交互信息，Cross-Encoder 精度高但慢，所以两阶段使用……" |
| 深挖题（问"如果……会怎样"） | 问题分析 → 当前设计的应对 → 可能的改进方向 | 1-2 分钟 | "这是一个很好的问题。当前设计通过……来应对，但如果出现……场景，我的方案会遇到……，改进思路是……" |

## 面试前盲区自查清单

- [ ] RAG 全链路流程能脱稿讲清楚（离线+在线两段）
- [ ] BGE-M3 的三大核心能力（多语言、多粒度、多功能）
- [ ] RRF 公式可以手算演示
- [ ] RRF 优于加权平均的理由（分数尺度不同）
- [ ] 当前混合检索架构 vs BGE-M3 原生混合的区别
- [ ] top_k 的完整链路（召回 10 → 融合 → 精排 → LLM 取 3）
- [ ] IVF_FLAT vs HNSW 的选型逻辑（数据量级决定）
- [ ] MinerU pipeline 后端的 content_list_v2 输出格式
- [ ] context 放入 system prompt 而非 user message 的原因
- [ ] 流式接口为什么不重试（已发送不可撤回）
- [ ] 4xx 错误为什么不重试（永久故障，重试无意义）
- [ ] 自动探测的四项功能及降级策略
- [ ] 异步预加载的三个阶段及容错机制
- [ ] 代码已知瑕疵：参数欺骗、硬编码、单 provider 绑定
- [ ] 单例模式的竞态条件及改进方案
