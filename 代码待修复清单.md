# 项目二 代码待修复清单

> 创建时间：2026-05-25
> 状态：仅记录，答辩后再修
> 优先级：P0=必须修 P1=建议修 P2=优化项

---

## P0 — 必须修复

### 1. llm_router.py — 参数欺骗（_create_client 函数）

**位置**：第 90-107 行
**问题**：函数签名 `_create_client(self, provider: str, host: str, model: str)` 声明了 host 和 model 参数，但 sglang 分支内部完全忽略传入的参数值，硬编码了地址和 model="default"。
**影响**：签名和实现不一致，让人误以为 host/model 是用传入的值。如果换地址或模型名，必须改代码，不能只改配置。
**方案**：把硬编码的地址改用 settings 配置项传入，model 用 settings.SGLANG_MODEL 而非 "default"。

### 2. llm_router.py — IP/端口硬编码散落

**位置**：第 46 行、第 95 行
**问题**：`"http://172.23.190.86:30000"` 在文件中出现了至少两处（_check_service 和 _create_client），不是从 settings 集中读取的。
**影响**：换环境时必须搜遍代码改所有出现的地方，容易漏改。
**方案**：统一从 settings 中读取基地址，一处定义多处引用。

### 3. llm_router.py — model="default" 占位符

**位置**：第 96 行
**问题**：client.model = "default"，这个值会被塞进 API 请求体的 model 字段。SGLang 虽然忽略这个字段，但"default"没有任何语义含义，别人看代码不知道这是什么。
**影响**：如果以后换了不忽略 model 字段的服务，这个值会出问题。同时让人读代码时困惑。
**方案**：用 settings.SGLANG_MODEL 的真实值，或在注释中说明"此字段被 SGLang 忽略，填任意非空值"。

### 4. llm_client.py — 单 provider 绑定

**位置**：第 29-53 行（__init__）
**问题**：LLM_PROVIDER 写死一个值，一个 LLMClient 实例只能连一个云端服务。不像 LLMRouter 可以在 SGLang 和 Ollama 之间智能切换。
**影响**：不支持多云端路由（如简单问题调便宜的 API，复杂问题调贵的 API；或者 A/B 测试不同模型效果）。
**方案**：参考 LLMRouter 的多引擎模式，把云端 API 也纳入智能路由体系。

---

## P1 — 建议修复

### 5. llm_client.py — 429 限流错误未重试

**位置**：第 119-144 行（_generate_openai_compatible 的重试逻辑）
**问题**：`raise_for_status()` 统一处理了所有 HTTP 错误，4xx 全部不重试。但 429（Too Many Requests/限流）不同于其他 4xx——它不是永久故障，等待几秒后大概率能恢复，应该纳入重试策略。
**影响**：频繁请求时被限流，直接抛异常中断，没有等待重试的机会。
**方案**：在 except 块中单独判断 `response.status_code == 429`，纳入指数退避重试逻辑。

---

## P2 — 优化项

（后续发现再补充）
