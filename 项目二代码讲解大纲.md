# 项目二：招股说明书智能问答系统 — 代码讲解大纲

## 面试讲解总时长建议：45-60分钟
## 结构：离线部分 + 在线部分，按数据流方向

---

# 第一部分：整体架构概览（3分钟）

## 1.1 一句话讲清楚项目
> "这是一个招股说明书的RAG问答系统，从PDF解析到最后生成答案，全流程自动化。"

## 1.2 整体架构图（脱稿画）
```
PDF文件 → parse_pdf.py → chunk_text.py → generate_embeddings.py → import_to_milvus.py
（离线管道）                                                                        ↓
用户 → POST /api/v1/chat → chat.py → retrieval.py → embedding_service → Milvus检索
                        ↑                     ↓                          ↓
                        |              Reranker（可选） ← BM25混合检索（可选）
                        |                     ↓
                        |              LLM Router → LLM Client → 生成答案
                        |_____________________________|
```

## 1.3 两个核心设计理念（贯穿整个讲解）
1. **自动探测+智能降级** — 不是配置开关，是运行时自动检测
2. **幂等设计** — 每次重新运行离线管道，旧数据不会污染

---

# 第二部分：离线管道（15分钟）

## 模块1：PDF解析 — parse_pdf.py（4分钟）

### 1.1 做了什么事
- 输入：招股说明书1.pdf（约300-400页）
- 输出：data/parsed/招股说明书_原始文本.json
- 每个页面：page_num + type(text/table) + text + section

### 1.2 关键设计点
- **文本提取：** MinerU pipeline 后端，自带版面分析，自动识别内容类型，按阅读顺序输出
- **表格检测：** MinerU版面分析模型自动识别表格区域，输出结构化的行列数据
  - 关键：表格不是"识别出来的字符串"，而是有行有列的二维数组
- **页面分类：** 页眉跳过（`PDF_HEADER_KEYWORDS`）→ 清空文本页的表格内容 → 保留纯文本
- **章节检测：** 正则匹配"第X节""第X章""X、"等章节标题 → 逐页分组
- **质量检查：** 最少页数 + 空白页比例，提前发现PDF解析异常

### 1.3 面试要点
- MinerU pipeline后端自带版面分析，自动按阅读顺序输出，解决多栏排版混排问题
- MinerU自动识别表格类型，输出结构化行列数据，不需要手动坐标裁剪
- 章节检测是后续分块的基础——按章节分组保持语义连贯性

## 模块2：文本分块 — chunk_text.py（5分钟）

### 2.1 做了什么事
- 输入：解析后的JSON（按页组织的文本+表格）
- 输出：data/chunks/招股说明书_分块.jsonl（JSONL格式）
- 每个chunk：chunk_id + text + type + page_start + page_end + section + source_pdf + char_count

### 2.2 关键设计点
- **按章节分组：** 同章节的文本页合并 → 段落化 → 逐段累加切chunk
  - 为什么？同一章的内容语义相关，跨章分块会把不同主题混在一起
- **CHUNK_MAX_CHARS = 800：** BGE-M3最大512 token ≈ 800字符，段落为单位，不过度切割
- **CHUNK_OVERLAP = 150（句子边界感知）：**
  - 原理：上一chunk末尾150字符作为下一chunk开头
  - 创新点：不是硬切150，而是用 `rfind("。")` 找句号，从句子边界切
  - 举例：假设chunk1以"公司2023年营收为1.2亿元，同比增长15"结尾 → overlap保留"年同比增长15"整句
  - 表格不走overlap：表格每行是独立数据，加overlap只重复表头
- **split_long_paragraph：** 段落 > 800字符时内部二次切分
- **JSONL格式：** 每行一条JSON，可流式处理大文件

### 2.3 面试要点
- overlap的作用：边界信息不丢失
- 句子边界感知 vs 硬切
- 表格单独处理的原因：结构化数据，不走文本分块逻辑
- 质量报告函数：统计chunk数量、大小分布、overlap覆盖对数

## 模块3：向量化 — generate_embeddings.py（3分钟）

### 3.1 做了什么事
- 输入：分块后的JSONL
- 输出：data/chunks/招股说明书_带向量.jsonl（包含1024维向量）
- 模型：BGE-M3（100+语言，1024维，Dense+Sparse+ColBERT三模式）

### 3.2 关键设计点
- **Batch编码：** batch_size=32，每批转成向量，GPU利用率高
- **normalize_embeddings=True：** 归一化后内积=余弦相似度
- **文本向量解耦存储：** JSONL存文本+元数据，向量在embedding字段里（但Python不用分离存储）
- **失败容错：** 单条失败只记录日志（`try-except`继续），不中断整个流程
- **质量报告：** 向量化成功率 > 95%才认为合格

### 3.3 面试要点
- 为什么BGE-M3而不是M3E/OpenAI？多语言+三模式+1024维
- normalizae_embeddings的意义：消除文本长度对相似度的干扰
- batch_size为什么是32？RTX 4060 8GB显存安全值

## 模块4：导入Milvus — import_to_milvus.py（3分钟）

### 4.1 做了什么事
- 输入：带向量的JSONL
- 输出：Milvus数据库 prospectus 集合
- 索引类型：IVF_FLAT（nlist=128）

### 4.2 关键设计点
- **幂等设计：** 导入前检查chunk_id是否已存在，重复则跳过
- **IFV_FLAT索引：** 聚类128个中心点，先粗搜再精搜，性价比最高
- **Schema：** 7字段（auto_id + chunk_id + text + page_range + section + source + embedding(1024维)）
- **批量导入：** batch_size=100，减少网络往返
- **REBUILD_COLLECTION标志：** 控制重建还是增量导入

### 4.3 面试要点
- 为什么IVF_FLAT不是HNSW？百万级以下IVF性价比高，HNSW内存大
- 幂等的实现方式：导入前查chunk_id
- 7字段设计理由：检索结果需要包含完整元数据

---

# 第三部分：在线服务（25分钟）

## 整体调用链
```
用户 → POST /api/v1/chat → chat.py(ChatRequest) → RetrievalService.query()
  → embedding_service.encode(query) → Milvus检索
  → (可选) BM25混合检索 → RRF融合
  → (可选) Reranker精排
  → LLM Router(智能分流) → LLM Client生成答案
  → SSE流式/JSON返回
```

## 模块5：服务入口 — main.py + chat.py（3分钟）

### 5.1 main.py
- FastAPI应用，挂载3个中间件：CORS(最外) → Logging → RateLimit(最内)
- /health健康检查：返回status=healthy + 版本号
- **启动事件不加载模型：** 模型在首次请求时懒加载

### 5.2 chat.py
- POST /api/v1/chat：普通问答，返回完整JSON（answer + sources）
- POST /api/v1/chat/stream：流式问答，SSE格式[DONE]结束
- Pydantic模型校验请求格式

### 5.3 面试要点
- 中间件顺序为什么CORS在最外层
- 为什么启动事件不加载模型（懒加载设计）
- 为什么单进程+异步（模型加载一次，不浪费显存）

## 模块6：检索总调度 — retrieval.py（6分钟）

### 6.1 做了什么事
- RAG流程的总调度
- 内部包含5个子模块调度：Query理解 → Embedding → 向量检索 → 混合检索 → Reranker

### 6.2 关键设计点 — **auto-probe模式（核心亮点）**
- `__init__`中调用4个`_probe_xxx`方法，检测功能可用性
- `self.features = {"query_understanding": False, "cache": False, "hybrid_retrieval": False, "reranker": False}`
- 每个probe有`try-except`，失败只记录日志，features保持False
- 运行时不判断开关，通过features字典决定是否调用

### 6.3 _run_retrieval_pipeline流程
1. **Query理解：** 意图识别 + 实体提取 + 消歧判断
2. **Query转向量：** embedding_service.encode()
3. **向量检索：** Milvus search，返回Top-K
4. **混合检索（可选）：** BM25关键词检索 → RRF融合（k=60）
5. **Reranker精排（可选）：** Cross-Encoder二次排序，Top-100 → Top-5
6. **Cache检查（可选）：** Redis缓存命中直接返回

### 6.4 面试要点
- probe设计：不需要开关，系统自动判断
- Reranker异步预加载：probe只检查文件存在，后台线程加载
- 混合检索的probe：检查文档数 > 100 才启用
- RRF公式：`score = weight / (k + rank)`，解决向量分和BM25分不可比问题

## 模块7：Query理解 — query_understanding.py（3分钟）

### 7.1 做了什么事
- IntentType枚举：FINANCIAL / RISK / BUSINESS / LEGAL / MANAGEMENT / COMPARISON / GREETING
- 意图识别：关键词匹配 + jieba分词
- 实体提取：时间、金额、人名（jieba.posseg）
- 消歧判断：对模糊问题（如"财务数据"）返回消歧提示

### 7.2 关键设计点
- **规则引擎：** 不是训练模型，是关键词+正则规则
- **消歧提示：** "您想了解哪个时间段的财务数据？"（用户最喜欢的点）
- **IntentResult结构：** intent + entities + sub_questions + disambiguation_needed + disambiguation_prompt

### 7.3 面试要点
- 为什么用规则不用训练模型？数据不够 + 规则够用
- 消歧的设计：不猜用户意图，直接问
- jieba.posseg的作用：提取"2023年"（t时间）、"1.2亿元"（m金额）

## 模块8：重排序 — reranker.py（3分钟）

### 8.1 做了什么事
- Cross-Encoder精排：Top-100粗排 → Top-5精排
- 模型：bge-reranker-base

### 8.2 关键设计点 — **异步预加载**
- `_async_preload_reranker`：后台线程加载模型
- `__init__`中 `threading.Thread(target=_lazy_load_reranker).start()`
- `_get_reranker()`：首次调用时 `_reranker_loaded.wait()` 等待加载完成
- **容错：** 模型加载失败 → self.model保持None → 所有rerank调用返回空 → 只用向量检索结果

### 8.3 面试要点
- 异步预加载的核心目的：服务先启动，模型后台加载
- 面试话术："同步加载模型失败服务崩溃，异步加载模型失败只是少一个功能"
- Bi-Encoder vs Cross-Encoder对比表
- RerankResult结构：document + relevance_score + original_rank + new_rank

## 模块9：LLM智能路由 — llm_router.py + llm_client.py（5分钟）

### 9.1 做了什么事
- LLMRouter：智能选择引擎（SGLang vs Ollama）
- LLMClient：统一封装多种LLM API调用

### 9.2 LLMRouter关键设计
- **自动发现：** `_check_service()` 发GET请求检测SGLang/Ollama是否在线
- **智能分流：**
  - 只有一个服务 → 全部走它
  - 两个都有 → `_analyze_complexity()` 判断：
    - 有历史对话 → 复杂 → SGLang
    - 含"分析""对比""为什么" → 复杂 → SGLang
    - 超长问题 → 复杂 → SGLang
    - 一问一答（正则匹配） → 简单 → Ollama
    - 默认 → 简单 → Ollama
- **故障降级：** 主引擎失败 → `try-except` 切备用

### 9.3 LLMClient关键设计
- 支持provider：sglang / ollama / siliconflow / deepseek
- 指数退避重试：1s → 2s → 4s
- 流式/非流式统一接口
- `__new__`绕过`__init__`的hack：LLMRouter需要同时持有两个Client

### 9.4 面试要点
- 为什么做智能分流？4060 8GB显存有限，轻量模型省资源
- 复杂度判断的规则可靠性
- 故障降级的链式设计：主→备用→抛异常→上层兜底
- 为什么自己封装不用LangChain：可控+面试加分

## 模块10：辅助模块概要（3分钟）

### 10.1 BM25混合检索 — bm25_retriever.py
- 经典BM25算法：k1=1.5, b=0.75
- 中文分词：jieba
- 解决的问题：向量检索对精确匹配（代码编号、否定词"不包含"）能力弱

### 10.2 Milvus Client — milvus_client.py
- 连接Milvus gRPC服务（端口19530）
- search方法：定义输出字段、top_k参数

### 10.3 Redis Cache — redis_client.py
- 缓存查询结果，相同query直接返回
- 缓存键：MD5(query + top_k)

### 10.4 中间件
- LoggingMiddleware：记录每次请求的Method/Path/耗时/状态码
- RateLimitMiddleware：60秒100次，IP级别限流

---

# 第四部分：面试问答速查（5分钟）

> 这个在后续的完整文稿中会展开为20+题，这里先列重点：

### 必问TOP10
1. RAG流程讲一下？ → **整体调用链**
2. 分块大小为什么800？overlap为什么150？ → **BGE-512token≈800字符 + 20%经验值**
3. 为什么用IVF_FLAT不用HNSW？ → **百万级性价比**
4. BGE-M3有什么特别？ → **多语言+三模式+与reranker兼容**
5. 混合检索怎么融合的？ → **RRF，忽略分数只看排名**
6. Bi-Encoder和Cross-Encoder区别？ → **双塔独立编码 vs 交叉拼接编码**
7. LLM Router怎么分的？ → **复杂度分析规则**
8. 异步预加载解决了什么问题？ → **服务启动不崩溃**
9. 自动探测怎么实现的？ → **4个probe方法**
10. 幂等怎么保证？ → **导入前检查chunk_id**

---

# 总结：项目的三大亮点

1. **自动探测+智能降级** — Reranker/混合检索/缓存/Query理解全部自动检测，不需要手动开关
2. **智能LLM分流** — 在8GB显存有限条件下，用规则判断复杂度做引擎切换，兼顾速度和质量
3. **幂等+容错的工程素养** — 离线管道的幂等导入、在线服务的懒加载+异步预加载、中间件链式设计
