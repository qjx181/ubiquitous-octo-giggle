# 项目二：招股说明书智能问答系统 — 面试项目介绍话术

> 使用方式：面试官说"介绍一下你的项目"时，按这个结构脱稿讲。
> 时长：5-8 分钟（不被打断），每个环节面试官可能随时追问。

---

## 🎯 第一段：一句话定位（30秒）

> "我的项目二是一个**面向招股说明书的 RAG 智能问答系统**。用户上传 PDF 招股书后，系统自动完成解析、分块、向量化、导入向量数据库，然后用户就可以用自然语言提问，系统基于文档内容给出带引用来源的答案。"

**考察点**：能不能在 30 秒内让别人知道你做的是什么。

**追问预防**：不要主动提技术细节（BGE-M3、Milvus、IVF_FLAT），留给后面展开。

---

## 🏗 第二段：整体架构（1分钟）

> "整体架构分**离线管道**和**在线服务**两段。
>
> **离线管道**处理知识构建：PDF 解析 → 文本分块 → Embedding 向量化 → 导入 Milvus。跑一次就行，知识库不变就不需要重跑。
>
> **在线服务**处理用户查询：FastAPI 接收请求 → 检索管道（Query理解 → Embedding编码 → 向量检索 → 可选 BM25 混合检索 → 可选 Reranker 精排）→ LLM 生成答案 → 返回前端。
>
> 核心设计理念是两个：**自动探测+智能降级**——系统启动时自动检测哪些功能可用，不可用的静默跳过，不手动配开关；**幂等设计**——离线管道重跑不会产生脏数据。"

**考察点**：有没有大局观，能不能把复杂系统用几分钟讲清楚。

**追问预防**：如果面试官问"为什么分两段"——答"解耦，知识构建和在线问答互不影响，换知识库不用改代码。"

---

## 📥 第三段：离线管道要点（1.5分钟）

> "离线管道四个步骤：
>
> **① PDF 解析**：用 MinerU 的 pipeline 后端解析 PDF，自带版面分析能力，能自动识别正文/表格/图片/页眉页脚等元素，自动丢弃非正文内容。比 PyMuPDF 多了 OCR 能力，能处理扫描件 PDF。
>
> **② 分块**：按章节分组，800 字符一段，150 字符 overlap。overlap 不是硬切的——用 `rfind('。')` 从句号边界切，避免中间腰斩句子。表格不走 overlap。
>
> **③ 向量化**：BGE-M3 模型，1024 维输出，`normalize_embeddings=True` 做归一化。batch_size=32 是 8GB 显存的安全值。
>
> **④ Milvus 导入**：IVF_FLAT 索引（nlist=128），7 字段 schema（id、chunk_id、text、page_range、section、source、embedding）。幂等检查——导入前查已有哪些 id，重复的跳过。"

**考察点**：工程细节——参数值、选型理由、边界处理。

**面试官可能追问：**
- "分块大小 800 怎么来的？" → BGE-M3 最大 512 token，约 800 字符
- "为什么 IVF_FLAT 不是 HNSW？" → 几百条数据，HNSW 内存 5 倍以上，杀鸡用牛刀
- "为什么选 BGE-M3？" → 多语言、1024 维、有配套 reranker
- "幂等怎么实现的？" → 导入前检查 chunk_id 是否已存在

---

## 🌐 第四段：在线服务要点（3分钟）— 重点

> "在线服务从 FastAPI 入口开始，`POST /chat/` 和 `POST /chat/stream` 两个接口，一个一次性返回完整 JSON，一个 SSE 流式逐 token 推送。
>
> **核心在 RetrievalService**：初始化时做 4 次自动探测——Query 理解能不能导入、Redis 能不能连上、文档数是否在混合检索范围内、Reranker 模型在不在本地。都能 try-except 包着，失败功能就不启用，服务不崩溃。
>
> **检索管道 5 步走：**
>
> 第一步，**Query 理解**。纯规则实现——意图识别（9 种意图打分制）、实体提取（正则+jieba）、查询改写（"去年"→"2023年"）、消歧追问（财务问题没时间→追问"您想了解哪个时间段？"）。
>
> 第二步，**向量检索**。BGE-M3 编码成 1024 维向量，去 Milvus 做近似搜索。
>
> 第三步，**混合检索**（可选）。自建 BM25 做关键词检索，和向量结果通过 RRF 融合。RRF 公式 `score = weight / (k + rank)`，不看分数只看排名，解决两个系统分数量级不一致的问题。
>
> 第四步，**Reranker 精排**（可选）。Cross-Encoder（bge-reranker-base）对 top_k 结果重排序。模型加载做了三级容错——probe 检查文件存在、后台线程异步预加载、首请求懒加载兜底。全失败就跳过 rerank，用原始排序。
>
> 第五步，**LLM 生成**。LLMRouter 根据问题复杂度选引擎——复杂问题走 SGLang（强模型），简单问题走 Ollama（轻量模型）。主引擎失败自动切到备用引擎。LLMClient 封装了指数退避重试（只重试超时和连不上，4xx 不重试），context 拼进 system prompt 而不是 user message。"

**考察点**：核心设计决策、容错策略、降级路径——这是区分"做过"和"真正理解"的地方。

**面试官可能追问：**
- "探测失败服务会怎样？" → 单个功能不启用，服务正常跑
- "Reranker 挂了怎么办？" → model=None，降级用原始分数排序
- "RRF 为什么不用加权平均？" → BM25 分几十，向量分 0~1 没法直接加
- "context 为什么放 system 不是 user？" → system 权重更高，模型更重视
- "4xx 为什么不重试？" → 永久故障（Key 过期、模型名不对），重试没用
- "流式为什么不重试？" → 已发的 token 撤不回来，靠 LLMRouter 故障转移

---

## ✨ 第五段：亮点总结（30秒）

> "这个项目我理解最深刻的三个设计：
>
> **第一，自动探测+智能降级**。不是手动配开关，而是系统启动时自己检测环境。四个增强功能（Query 理解、缓存、混合检索、Reranker）各自独立，失败的不会拖累整个服务。
>
> **第二，异步预加载三级容错**。Reranker 模型 1GB 加载要好几秒，同步加载会让服务启动慢 5-10 秒。异步在后台加载，失败也不崩溃。
>
> **第三，RRF 融合解决分数尺度问题**。向量分和 BM25 分不在一个量级，RRF 靠排名不靠分数，优雅地解决了这个问题。"

**考察点**：能不能总结出自己项目的亮点，而不是等面试官来发现。

---

## ⚠️ 面试官可能会打断的方向

| 打断方向 | 怎么应对 |
|---------|---------|
| "详细讲讲 RRF 公式" | 手写公式 + 手算例子（排第3拿多少分）|
| "你说自动探测，具体探测什么？" | 4 个 probe 逐个说，每个 probe 做什么、怎么容错 |
| "异步预加载和懒加载什么关系？" | 三级容错：probe→异步加载→懒加载兜底 |
| "BM25 你参数怎么设的？" | k1=1.5, b=0.75，业界标准值 |
| "为什么不直接调 LLM API 要包一层？" | 统一接口 + 重试 + 指数退避 + 多 provider 兼容 |

---

## 📋 面试前 10 分钟速记卡

```
一句话：PDF招股书 → RAG智能问答 → 带引用来源
架构：离线4步 + 在线5步
4个probe：Query理解 / Redis缓存 / BM25混合检索 / Reranker
RRF：score = weight/(k+rank), k=60
BGE-M3: 1024维, normalize, batch=32
IVF_FLAT: nlist=128
context放system不放user
4xx不重试，流式不重试
代码瑕疵：IP硬编码、参数欺骗、单provider绑定
```
