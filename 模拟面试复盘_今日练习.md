# 模拟面试复盘 — 今日练习题目（6题）

> 日期：2026-05-25
> 说明：记录了模拟面试中答错/卡住的问题、正确回答和需要强化的知识点

---

## 题目1：项目整体流程

**❌ 你答的问题：** 漏了 Query 理解、LLM 路由，没提自动探测。步骤顺序有跳跃。

**✅ 正确回答：**

> "所有增强功能——包括 Query 理解、Redis 缓存、BM25 混合检索、Reranker——都是**启动时自动探测**的，不配开关。探测失败那个功能就不开，服务不会崩溃。
>
> 检索管道走 **7 步**：
>
> ① 查 Redis 缓存；
> ② Query 理解——意图识别、实体提取、改写、消歧；
> ③ BGE-M3 编码成 1024 维向量；
> ④ 向量检索召回到候选集；
> ⑤ 可选 BM25 混合检索，和向量结果做 RRF 融合——BM25 **不可用就跳过**；
> ⑥ 可选 Cross-Encoder Reranker 精排——Reranker **不可用也跳过**；
> ⑦ LLM 路由——判断简单复杂，选引擎，生成答案，流式返回。"

**强化点：**
- [ ] "加载失败" → 改成 "不可用就跳过"（体现设计的容错意图）
- [ ] 7 步顺序背熟：缓存→Query理解→编码→向量检索→BM25→Reranker→LLM

---

## 题目2：RRF 融合计算

**❌ 你答的问题：** 以为 RRF 看原始分数，公式记成了 `weight/(k+权重)`，不会手算。

**✅ 正确回答：**

**RRF 只看排名不看分数。** 公式：
```
score = weight / (k + rank)
```
- k = 60
- vector_weight = 0.7, bm25_weight = 0.3

**手算示例（背下来）：**
一条文档在向量检索排第 3，BM25 排第 7：
```
向量贡献：0.7 / (60 + 3) = 0.7 / 63 ≈ 0.0111
BM25贡献：0.3 / (60 + 7) = 0.3 / 67 ≈ 0.0045
总分：0.0156
```

**为什么不用加权平均？** BM25 得分可以到几十，向量得分在 0~1，量级不同不能直接加。

**强化点：**
- [ ] 公式记对：`weight / (k + rank)`，分母是 k+排名，不是 k+权重
- [ ] 手算例子背熟：排第 3 → 63，排第 7 → 67
- [ ] 说清楚：RRF 不看分数，只看排名

---

## 题目3：Reranker 异步预加载三级容错

**❌ 你答的问题：** "不会"——完全没复习到。

**✅ 正确回答：**

> "同步加载有两个问题：一是服务启动要等 3-5 秒，二是模型出问题整个服务直接崩溃。所以设计了**三级容错**。
>
> ① **Probe**：启动时检查模型文件在不在磁盘上（毫秒级），不在直接标记不可用。
>
> ② **异步预加载**：文件存在就开后台线程加载，不阻塞服务启动。服务秒级上线，/health 立刻可用。
>
> ③ **懒加载兜底**：第一个请求来时后台还没加载完，等最多 60 秒。超时了就同步加载一次，还是失败就彻底关闭 Reranker 功能。
>
> 失败后 `self.reranker` 保持 None，调用方发现为空，**跳过重排序步骤**，用原始排序结果——用户无感知。
>
> **关键不是省内存，是让服务先启动，模型挂了也不崩溃。** 这是典型的优雅降级——宁可少一个功能，不让整个服务停摆。"

**强化点：**
- [ ] 三级名称：Probe → 异步预加载 → 懒加载兜底
- [ ] 每个级别的作用和时间
- [ ] 失败后的降级路径：Reranker 不可用 → 跳过重排序
- [ ] "关键不是省内存，是服务先启动"

---

## 题目4：LLM 路由的复杂度判断

**❌ 你答的问题：** 说成了 query_understanding.py 的意图识别（正则判断），答非所问。

**✅ 正确回答：**

> "LLMRouter 的 `_analyze_complexity` 用了 **5 条规则**判断：
>
> ① **有对话历史** → 复杂（多轮对话需要更强模型）
> ② 命中 **COMPLEX_KEYWORDS**（分析、比较、对比、为什么等 24 个词） → 复杂
> ③ 问题**长度超过阈值**（默认 20 个字） → 复杂
> ④ 匹配 **SIMPLE_PATTERNS**（如"2024年营收是多少"、"什么是XXX"） → 简单
> ⑤ 以上都不满足 → 简单（兜底）
>
> 整体准确率大约 **80-90%**。后续改进方向是用小模型替代规则，提升到 95% 以上。"

**强化点：**
- [ ] 5 条规则背熟：历史→关键词→长度→简单模式→默认
- [ ] 和 QueryUnderstanding 的意图识别区分开（两个不同模块）
- [ ] 加上"后续改进方向"

---

## 题目5：4xx 重试策略 + 429 边界情况

**❌ 你答的问题：** "这个我没有复习到。"

**✅ 正确回答：**

> "我们的重试策略是**只重试瞬态故障**——网络超时和连接失败。因为 4xx 是客户端错误，重试多少次都一样错。
>
> 但有一个边界情况——**429 限流其实应该重试**，等几秒后大概率能成功。但当前代码用 `raise_for_status()` 统一处理了所有 HTTP 错误码，429 也没重试——**这是后续可以优化的地方。**"

**强化点：**
- [ ] 核心原则：只重试瞬态故障（超时、连不上），不重试永久故障（4xx）
- [ ] 429 是特例——限流等几秒就能恢复，应该重试
- [ ] "当前代码统一处理了，429 没重试——这是后续可以优化的"
- [ ] 主动指出代码不完美之处反而加分

---

## 题目6：缓存策略

**❌ 你答的问题：** "这个我也没有复习到。"

**✅ 正确回答：**

> "缓存键是 `MD5(question + content_type + top_k)`，命中直接返回，跳过整个检索管道。TTL = 1 小时。
>
> **需要澄清的 Query 不缓存**——因为返回的是追问模板，没有计算量。
>
> **流式接口也不走缓存**——流式输出的中间状态不适合存起来。"

**强化点：**
- [ ] 缓存键：MD5(question + content_type + top_k)
- [ ] TTL：1 小时
- [ ] 不缓存的两类：需澄清的 Query、流式接口

---

## 面试前必看·今日盲区清单

- [ ] RAG 完整 7 步流程（缓存→Query理解→编码→向量检索→BM25→Reranker→LLM）
- [ ] 自动探测的 4 个功能及降级策略
- [ ] RRF 公式手算（weight / (k + rank)）
- [ ] 异步预加载三级容错
- [ ] LLM 路由 5 条规则
- [ ] 4xx 不重试 + 429 应重试
- [ ] 缓存键设计 + 不缓存场景
- [ ] Context 放 system 不是 user
