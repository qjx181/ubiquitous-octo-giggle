# 招股说明书智能问答系统 — 开发工单

> 项目代号：Prospectus-QA
> 创建日期：2026-05-06
> 开发人员：___________
> 状态：⬜ 待开发

---

## 一、项目概述

### 1.1 项目背景

基于大语言模型（LLM），开发一个针对《招股说明书1.pdf》的智能问答系统，能够高效检索文档内容并生成准确、简洁的回答。

### 1.2 核心能力

| 能力 | 说明 |
|------|------|
| Query理解 | 意图识别、消歧、复杂问题分解、关键词抽象 |
| 检索与生成 | PDF解析 → 分块 → 向量化 → 语义检索 → LLM答案生成 |
| 用户体验 | 简洁的交互界面，支持输入、展示和反馈 |

### 1.3 技术栈推荐

| 组件 | 推荐方案 | 备选方案 |
|------|---------|---------|
| 后端框架 | FastAPI（复用已有经验） | Flask |
| 向量数据库 | ChromaDB（轻量，免部署） | Milvus（已有经验） |
| Embedding模型 | BGE-M3 / text2vec-large-chinese | sentence-transformers |
| LLM | Ollama（qwen2.5:7b 或 deepseek-r1:7b） | DeepSeek API |
| PDF解析 | PyMuPDF (fitz) | pdfplumber |
| 前端 | 纯HTML+JS或Streamlit | Gradio |
| 运行环境 | WSL2 Ubuntu | Windows原生 |

---

## 二、功能需求详述

### 2.1 Query理解模块

#### 2.1.1 意图识别

- 输入：用户原始问题（如 "公司去年赚了多少钱？"）
- 处理：调用LLM判断问题类型
- 输出：意图标签
- 意图分类示例：

| 意图标签 | 说明 | 示例 |
|---------|------|------|
| financial | 财务数据类 | "营业收入是多少？" |
| risk | 风险因素类 | "公司有什么经营风险？" |
| business | 业务介绍类 | "公司主要做什么业务？" |
| legal | 法律合规类 | "有没有未决诉讼？" |
| management | 管理层信息类 | "董事长是谁？" |
| greeting | 问候/闲聊 | "你好"、"谢谢" |
| unclear | 意图不明确 | 模糊提问 |

#### 2.1.2 消歧（歧义处理）

- 检测问题中的多义词或模糊表述
- 策略：
  - **上下文消歧**：结合对话历史判断
  - **追问消歧**：当无法确定时，反问用户以澄清
- 示例：
  - 用户问："收入是多少？" → 检测到缺少年份限定 → 追问"您想问哪一年的收入？"
  - 用户问："风险有哪些？" → 检测到"风险"可能指经营风险/财务风险/市场风险 → 追问"您想了解哪一类风险？"

#### 2.1.3 问题分解与抽象

- 将复杂问题拆解为多个原子子问题
- 提取关键实体和限定条件（公司名、年份、指标等）
- 示例：

| 用户原始问题 | 分解后的子问题 | 提取的关键信息 |
|-------------|--------------|---------------|
| "公司近三年的营收和净利润变化趋势如何？" | ① 2021年营收是多少？② 2022年营收是多少？③ 2023年营收是多少？④ 2021年净利润是多少？⑤ 2022年净利润是多少？⑥ 2023年净利润是多少？ | 公司名、2021/2022/2023、营收、净利润 |
| "公司有哪些风险因素，管理层如何应对？" | ① 公司披露了哪些风险因素？② 针对这些风险，管理层采取了什么措施？ | 风险因素、管理层应对 |

### 2.2 检索与生成模块

#### 2.2.1 PDF解析（离线）

- 输入：`招股说明书1.pdf`
- 处理：
  1. 使用 PyMuPDF 提取全文文本
  2. 保留文档结构（章节标题、页码）
  3. 输出结构化 JSON 或文本文件
- 输出：`招股说明书_原始文本.json`（含 page_num, section, content 字段）

#### 2.2.2 文本分块（Chunking）

- 策略：**语义分块 + 重叠窗口**
- 参数：

| 参数 | 推荐值 | 说明 |
|------|-------|------|
| chunk_size | 512 tokens | 每块文本长度 |
| chunk_overlap | 128 tokens | 相邻块重叠长度 |
| 分块策略 | 按段落优先，超出则截断 | 避免切断完整句子 |

- 输出：`招股说明书_分块.jsonl`（每行一个chunk，含 chunk_id, text, metadata）

#### 2.2.3 向量化与存储（离线）

- 使用 Embedding 模型将每个chunk转为向量
- 存储到向量数据库（推荐 ChromaDB，轻量免部署）
- 存储字段：

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一ID |
| text | string | 原始文本内容 |
| embedding | float[] | 向量表示 |
| metadata | dict | 包含 page_num, section, chunk_index 等 |

#### 2.2.4 检索流程（在线）

1. 用户输入问题
2. Query理解模块处理（意图识别+消歧+分解）
3. 对每个子问题/原始问题执行向量检索
4. 使用 **混合检索**（向量相似度 + BM25关键词）提升召回率
5. 可选的 **重排序**（Reranker）精排 top-k 结果
6. 合并去重后作为上下文

#### 2.2.5 答案生成

- 将检索到的相关chunks拼入 Prompt
- 调用 LLM 生成最终答案
- 要求：
  - **基于检索内容回答**，不随意编造（防幻觉）
  - **标注引用来源**（如 "根据招股说明书第X页"）
  - 如果检索不到相关信息，明确告知"未在招股说明书中找到相关内容"

### 2.3 用户体验模块

#### 2.3.1 交互界面

**推荐方案A：简易Web界面（纯HTML + FastAPI模板）**

- 页面布局：
  - 顶部：标题栏 + 系统说明
  - 中间：对话区域（消息气泡展示，用户问题与AI回答交替显示）
  - 底部：输入框 + 发送按钮
- 功能：
  - 支持多轮对话
  - 回答中高亮显示引用来源
  - 支持 Markdown 渲染（表格、列表、代码块）

**推荐方案B：Gradio/Streamlit（更快速搭建）**

- 适合快速原型验证
- 内置对话组件，少写前端代码

#### 2.3.2 反馈机制

- 每个回答下方提供：
  - 👍 有用 / 👎 没用 按钮
  - 评分数据记录到本地文件或日志
- 用途：后续分析哪些问题回答质量差，针对性改进

---

## 三、项目结构规划

```
招股说明书问答系统/
├── README.md                    # 项目说明
├── requirements.txt             # Python依赖
├── 开发工单.md                  # 本文件
│
├── offline/                     # 离线数据处理
│   ├── parse_pdf.py             # 1️⃣ PDF解析
│   ├── chunk_text.py            # 2️⃣ 文本分块
│   └── build_vector_store.py    # 3️⃣ 向量化 + 存储
│
├── online/                      # 在线服务
│   ├── main.py                  # FastAPI入口 / 启动脚本
│   ├── config.py                # 配置文件（模型名、向量库路径等）
│   ├── query_understanding.py   # Query理解模块（意图+消歧+分解）
│   ├── retrieval.py             # 检索模块（混合检索）
│   ├── llm_client.py            # LLM调用封装
│   ├── prompt.py                # Prompt模板组装
│   └── feedback.py              # 反馈记录
│
├── frontend/                    # 前端（可选）
│   └── index.html               # 简易Web页面
│
├── data/                        # 数据目录
│   ├── 招股说明书1.pdf          # 源文件（放这里）
│   ├── parsed/                  # 解析后的文本
│   ├── chunks/                  # 分块结果
│   └── chroma_db/               # ChromaDB持久化目录
│
└── logs/                        # 日志
    └── feedback.log             # 用户反馈记录
```

---

## 四、开发阶段划分

### 阶段一：数据准备（预计 1 天）

| 序号 | 任务 | 产出物 | 工作量 |
|------|------|--------|--------|
| 1.1 | 确认或获取 `招股说明书1.pdf` 源文件 | 源文件就位 | 0.5h |
| 1.2 | 编写 `parse_pdf.py`：PDF文本提取 | 结构化JSON文本 | 2h |
| 1.3 | 编写 `chunk_text.py`：文本分块 | 分块后的JSONL | 1h |
| 1.4 | 编写 `build_vector_store.py`：向量化+入库 | ChromaDB索引 | 2h |
| **里程碑** | 数据管道跑通，能对任意问题返回相关chunks | | |

### 阶段二：核心检索与生成（预计 2 天）

| 序号 | 任务 | 产出物 | 工作量 |
|------|------|--------|--------|
| 2.1 | 编写 `query_understanding.py`：意图识别 | 意图分类函数 | 2h |
| 2.2 | 编写消歧逻辑（追问/澄清） | 消歧函数 | 1.5h |
| 2.3 | 编写问题分解逻辑 | 子问题拆分函数 | 1.5h |
| 2.4 | 编写 `retrieval.py`：向量检索 | 检索函数 | 1h |
| 2.5 | 编写 `prompt.py`：Prompt模板 | Prompt模板 | 1h |
| 2.6 | 编写 `llm_client.py`：LLM调用 | LLM客户端 | 1h |
| 2.7 | 将以上串联：检索→拼Prompt→LLM生成 | 完整Pipeline | 1h |
| **里程碑** | 命令行可运行：输入问题 → 得到带引用的回答 | | |

### 阶段三：Web交互界面（预计 1 天）

| 序号 | 任务 | 产出物 | 工作量 |
|------|------|--------|--------|
| 3.1 | 搭建 FastAPI 应用，注册路由 | main.py 框架 | 1h |
| 3.2 | 编写 `config.py` 集中配置 | 配置文件 | 0.5h |
| 3.3 | 编写前端页面 `index.html`（对话UI） | Web界面 | 2h |
| 3.4 | 前后端对接：输入→检索→生成→展示 | 可用Web服务 | 1.5h |
| 3.5 | 编写 `feedback.py`：反馈记录 | 反馈功能 | 1h |
| **里程碑** | 打开浏览器即可使用完整的问答系统 | | |

### 阶段四：优化与收尾（预计 1 天）

| 序号 | 任务 | 产出物 | 工作量 |
|------|------|--------|--------|
| 4.1 | 测试不同问题的回答质量，调优检索参数 | 调优记录 | 2h |
| 4.2 | 添加 BM25 关键词检索（与向量检索混合） | 混合检索 | 1.5h |
| 4.3 | 添加错误处理和日志 | 稳定版 | 1h |
| 4.4 | 编写 `README.md` 项目说明 | 文档 | 0.5h |
| **里程碑** | 系统稳定可用，文档完备 | | |

---

## 五、关键技术设计说明

### 5.1 Query理解流程

```
用户输入问题
    │
    ▼
┌─────────────────┐
│ 意图识别          │  → 如果是问候/闲聊 → 直接回复，不走RAG
│ (LLM分类)         │  → 如果意图不明确 → 追问澄清
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ 消歧处理          │  → 检测模糊表述 → 追问或结合对话历史推断
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ 问题分解 & 抽象   │  → 拆分子问题 + 提取关键实体
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ 检索 + 生成      │  → 每个子问题分别检索 → 合并上下文 → LLM生成
└─────────────────┘
```

### 5.2 检索增强生成（RAG）流程

```
检索到的chunks
    │
    ▼
┌────────────────────────────────────┐
│ Prompt模板                          │
│                                     │
│ 你是一个招股说明书问答助手。          │
│ 请基于以下文档内容回答问题：          │
│                                     │
│ --- 相关文档内容 ---                 │
│ [chunk1]                            │
│ [chunk2]                            │
│ ...                                 │
│ ---                               ---│
│                                     │
│ 用户问题：{query}                    │
│ 要求：                              │
│ 1. 仅基于以上内容回答                │
│ 2. 标注引用来源（页码）              │
│ 3. 如果内容不足以回答，如实告知       │
└──────────────┬─────────────────────┘
               │
               ▼
          ┌────────┐
          │  LLM   │ → 生成最终答案
          └────────┘
```

### 5.3 防幻觉设计

- **来源约束**：Prompt 中明确要求仅基于检索内容回答
- **引用标注**：每个关键结论后面标注来源页码
- **无法回答处理**：检索不到相关内容时，回复"未在招股说明书中找到相关信息"
- **可选增强**：对LLM输出做来源校验，检查每个断言是否能在检索内容中找到对应

---

## 六、交付物清单

| 序号 | 交付物 | 说明 |
|------|--------|------|
| 1 | 完整项目源代码 | 含 offline/、online/、frontend/ 全部代码 |
| 2 | `requirements.txt` | Python依赖清单 |
| 3 | `README.md` | 项目说明文档（含启动方式） |
| 4 | 处理好的向量数据库文件 | ChromaDB 持久化目录 |
| 5 | （可选）部署说明 | 如何在服务器上部署运行 |

---

## 七、验收标准

| 验收项 | 通过标准 |
|--------|---------|
| PDF解析 | 能成功提取`招股说明书1.pdf`的全部文字内容，保留章节结构 |
| 分块 | 文本被合理切分成512 token左右的chunks，保留上下文连贯性 |
| 向量检索 | 输入与招股说明书相关的问题，能返回语义最相关的3-5个chunks |
| Query理解 | 能正确分类意图；对模糊问题能追问；对复杂问题能分解子问题 |
| 答案生成 | 回答准确、简洁、引用页码、不编造 |
| 交互界面 | 打开浏览器能使用，输入问题得到回答，展示来源 |
| 反馈记录 | 用户点击有用/没用后，数据记录到日志 |
| 边界情况 | 非相关文档问题 → 告知查不到；空输入 → 提示输入；问候 → 友好回复 |

---

## 八、附录：参考资源

- PyMuPDF 官方文档：https://pymupdf.readthedocs.io/
- ChromaDB 官方文档：https://docs.trychroma.com/
- FastAPI 官方教程：https://fastapi.tiangolo.com/zh/
- Ollama 模型管理：https://ollama.com/
- sentence-transformers 嵌入模型：https://www.sbert.net/
